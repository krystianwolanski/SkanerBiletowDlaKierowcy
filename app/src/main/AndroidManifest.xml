<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bus">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity
            android:name=".Preview"
            android:label="@string/title_activity_preview"
            android:theme="@style/NoActionBar"></activity>
        <activity android:name=".Connection_activity" />
        <activity
            android:name=".ui.login.trying"
            android:label="@string/title_activity_trying" />
        <activity
            android:name=".choose_connection"
            android:label="@string/title_activity_choose_connection"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".connection"
            android:label="@string/title_activity_connection"
            android:theme="@style/NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="com.google.zxing.client.android.SCAN" />
                <category android:name="android.intent.category.DEFAULT" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>