package com.bus.Integracyjne

import com.bus.Data.DBHelper
import com.bus.Data.Data
import com.bus.MainActivity.Companion.context
import com.bus.Model.Ticket
import com.bus.System.BusSystem
import org.junit.Test


class Tests
{
    @Test
    fun test()
    {
   /*     Data(context!!)
        val dbHelper = DBHelper(context)

        val busConnection = dbHelper.getBusConnection(0)
        val tickets: MutableList<Ticket>? = dbHelper.getListTicket(busConnection)
        val busConnection1 = dbHelper.getBusConnection(1)
        val tickets1:MutableList<Ticket> = dbHelper.getListTicket(busConnection1)

        val busSystem = BusSystem()
        tickets?.get(0)?.qrCode?.let { context?.let { it1 -> busSystem.scanTicket(it1, it) } }*/

    }
}

